include: ../../../analysis_options.yaml

linter:
  rules:
    # An example app is not a published library; documenting every private
    # widget would bury what the example is meant to show.
    public_member_api_docs: false
